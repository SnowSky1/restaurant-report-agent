# Python 直接依赖（精确锁定；传递依赖由这些包解析）
# 安装：  pip install -r requirements.txt

# --- LangGraph / LangChain ---
langgraph==1.0.5
langchain-openai==1.1.7

# --- Web API ---
fastapi==0.135.1
uvicorn==0.40.0

# --- MCP / HTTP ---
mcp==1.26.0
httpx==0.28.1

# --- Config / models ---
pydantic==2.12.5
pydantic-settings==2.12.0
python-dotenv==1.0.0

# --- CLI / output ---
rich==14.2.0
