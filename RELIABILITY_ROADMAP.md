# 可靠性状态与后续路线

本文从 0.1.0 开始只记录仍未完成的生产化工作。历史文档中关于“硬编码 Mock、图不执行、无测试、CORS 全开放、前端空白”等描述已不再成立。

## 0.1.0 已完成

- FastAPI `/api/analyze` 真实运行完整 LangGraph。
- 高德 REST 为主、MCP 可选，支持 `auto / real / mock` 三种数据模式。
- 每次外部数据调用记录真实/模拟来源，前端和报告均显示降级原因。
- `location → parallel_data → competition_analysis → revenue_simulation → chart → report` 图结构完整。
- CompeteAI 启发的多主体、消费者分群、三轮竞争响应和三种定价情景已实现。
- DashScope OpenAI-compatible 主模型与 Claude/Krill `gpt-5.6-luna` 备用链路均已实测。
- 前端首页、地图选择、五个报告页和设置页均接入真实 API；报告刷新期间使用 sessionStorage 保持。
- 后端单元/集成测试、前端 ESLint、生产构建、生产依赖审计和浏览器端到端测试均通过。
- CORS、输入校验、错误脱敏和 Git 密钥隔离已实现。

## 当前边界

- 这是可运行的本地完整产品，不等同于互联网生产部署。
- 高德 POI 为有上限的接口样本；配额、产品权限和网络状态会影响真实覆盖率。
- 营收模型需要真实 POS、成本、租金和复购数据校准。
- 报告仅在当前浏览器标签会话中持久化，尚无账号与数据库。

## 下一阶段：生产化

### P1：可观测与持久化

- 结构化日志、request ID、每节点耗时和外部调用指标。
- 接入 OpenTelemetry/LangSmith/Langfuse 之一，记录图、工具与模型 span。
- 增加数据库、报告历史、导出和用户隔离。

### P1：接口治理

- 按错误类型实施指数退避、熔断与供应商健康状态。
- 对 geocode、天气和 POI 增加带 TTL 的缓存，降低配额消耗。
- 部署环境增加鉴权、速率限制、请求大小限制和审计日志。

### P2：模型与评测

- 建立真实地址 golden set、事实覆盖 rubric 和 LLM-as-judge 回归集。
- 将模型、提示词和模拟参数版本化；在合并前比较质量、成本和时延。
- 用实际经营数据拟合需求、容量和成本参数，并记录置信区间。

### P2：交付

- 增加 Docker Compose、CI 和部署健康探针。
- 加入 PDF/Excel 导出、报告共享和定期刷新。
- 用浏览器测试覆盖移动端、暗色模式和异常网络。

生产验收应至少定义：成功率、降级率、p95 时延、真实数据覆盖率、报告质量分和单次分析成本。
