# 0.0.0 可靠性状态与后续路线

## 当前基准已完成

- 单仓库保存后端、前端、测试、CI 与文档，版本统一为 `0.0.0`。
- 位置失败时 LangGraph 立即停止；完成状态区分 `complete / degraded / failed`。
- LLM 叙述输出采用白名单和大小限制，不可修改规则证据字段；提示词明确隔离不可信业务数据。
- 营收模型覆盖全部支持业态，具有可行性闸门、单变量客单价敏感性和固定成本不随营收缩放的投影。
- 高德请求具有共享连接、TTL 缓存、指数退避、并发边界、调用指标与样本范围说明。
- API 默认不落盘，支持可选令牌、请求频率/分析并发限制、安全响应头和显式就绪探测。
- 前端设置、主题、默认半径、当前地址检测、输入验证、取消操作及降级提示均与后端语义一致。
- 后端测试、前端逻辑测试、ESLint、生产构建、生产依赖审计和 GitHub Actions CI 均纳入验收流程。

## 仍属于产品演进，而非 0.0.0 缺陷

- 使用真实 POS、租金、人工、菜单毛利和复购数据拟合参数并给出置信区间。
- 引入数据库、账号隔离、报告历史、分享以及 PDF/Excel 导出。
- 接入 OpenTelemetry 等完整 span 系统，建立真实地址 golden set、质量/成本/时延回归基线。
- 为公网部署增加反向代理、TLS、持久化限流和集中式审计日志。

生产验收还需由业务方定义：成功率、降级率、p95 时延、真实数据覆盖率、报告质量分和单次分析成本。
